spring:
  application:
    name: service-consumer-sys
  #  profiles:
  #    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 139.224.83.117:8848
      config:
        server-addr: 139.224.83.117:8848
        file-extension: yaml
feign:
  hystrix:
    enabled: true

rocketmq:
  producer:
    isOnOff: on
    groupName: ${spring.application.name}
    namesrvAddr: 139.224.83.117:9876
    maxMessageSize: 4096
    sendMsgTimeout: 3000
    retryTimesWhenSendFailed: 0
  consumer:
    isOnOff: on
    groupName: ${spring.application.name}
    namesrvAddr: 139.224.83.117:9876
    topics: KarlTopic
    consumeThreadMin: 20
    consumeThreadMax: 64
    consumeMessageBatchMaxSize: 1